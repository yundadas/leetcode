class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        newjeans = []
        for i in range(0,n):
            newjeans.append(nums[i])
            newjeans.append(nums[i+n])
        return newjeans
